# Copy to `.env` in this project root (gitignored) OR use ~/.grok/.env
# Never commit real keys.

# console.x.ai prepaid API credits (not SuperGrok chat)
XAI_API_KEY=

# Optional aliases
# GROK_API_KEY=

# OpenRouter (when provider = OpenRouter)
# OPENROUTER_API_KEY=

# OpenAI-compatible / local (optional)
# OPENAI_API_KEY=
